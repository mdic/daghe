[project]
name = "check-updates"
version = "0.1.0"
description = "Internal utility to check Python package updates"
dependencies = [
    "httpx>=0.27.0",
]
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
