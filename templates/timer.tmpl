[Unit]
Description=DaGhE Timer for ${NAME}

[Timer]
OnCalendar=${CALENDAR}
RandomizedDelaySec=${RANDOM_DELAY}
Persistent=true

[Install]
# Standard target for systemd user timers
WantedBy=default.target
